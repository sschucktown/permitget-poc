{
  "version": 2,
  "builds": [
    { "src": "api/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "handle": "filesystem" },

    { "src": "/dashboard", "dest": "/dashboard.html" },

    { "src": "/api/(.*)", "dest": "/api/$1.js" }
  ]
}
